---
- hosts: all
  tasks:
    - name: ensure nginx is at the latest version
      yum: name=nginx state=latest

    - name: copy files
      copy:
        src: /etc/ansible/roles/nginx.conf
        dest: /etc/nginx/nginx.conf

    - name: start nginx
      service:
          name: nginx
          state: started
